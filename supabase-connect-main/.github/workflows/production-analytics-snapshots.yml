name: Production Analytics Snapshots

on:
  schedule:
    # GitHub cron uses UTC: 00:00 UTC = 03:00 EAT (Africa/Dar_es_Salaam, UTC+3)
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  generate-production-analytics-snapshots:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Trigger production analytics snapshots
        shell: bash
        env:
          PRODUCTION_SUPABASE_GATEWAY_JWT: ${{ secrets.PRODUCTION_SUPABASE_GATEWAY_JWT }}
          PRODUCTION_ANALYTICS_SCHEDULER_SECRET: ${{ secrets.PRODUCTION_ANALYTICS_SCHEDULER_SECRET }}
        run: |
          set -euo pipefail

          test -n "$PRODUCTION_SUPABASE_GATEWAY_JWT"
          test -n "$PRODUCTION_ANALYTICS_SCHEDULER_SECRET"

          response="$(
            curl --fail-with-body --silent --show-error \
              --request POST \
              --header "Authorization: Bearer ${PRODUCTION_SUPABASE_GATEWAY_JWT}" \
              --header "X-Analytics-Scheduler-Secret: ${PRODUCTION_ANALYTICS_SCHEDULER_SECRET}" \
              --header "Content-Type: application/json" \
              --data '{}' \
              "https://cbaxiiqlzrwvmuplhusm.supabase.co/functions/v1/analytics-snapshots"
          )"

          echo "$response" | jq -e '
            .success == true and
            .status == "completed"
          ' > /dev/null

          echo "$response" | jq '{
            success,
            status,
            run_id,
            pages_processed,
            churches_attempted,
            churches_succeeded,
            churches_failed
          }'
