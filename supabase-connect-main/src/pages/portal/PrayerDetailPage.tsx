import { useQuery } from "@tanstack/react-query";
import { ArrowLeft, ScrollText } from "lucide-react";
import { Link, useParams } from "react-router-dom";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Skeleton } from "@/components/ui/skeleton";
import { fetchPublishedPrayer } from "@/lib/content-display";
export default function PrayerDetailPage() { const { slug } = useParams(); const { data, isLoading, isError } = useQuery({ queryKey: ["published-prayer", slug], queryFn: () => fetchPublishedPrayer(slug!), enabled: !!slug }); if (isLoading) return <main className="mx-auto max-w-3xl px-4 py-8"><Skeleton className="h-96 rounded-3xl"/></main>; if (isError || !data) return <main className="px-4 py-10"><Card className="mx-auto max-w-3xl"><CardContent className="flex flex-col items-center py-14 text-center"><ScrollText className="h-10 w-10 text-muted-foreground"/><h1 className="mt-4 text-xl font-bold">Sala haijapatikana.</h1><Button asChild className="mt-5 min-h-11"><Link to="/portal/prayers">Rudi kwenye sala</Link></Button></CardContent></Card></main>; return <main className="min-h-full px-4 py-6 pb-28 lg:px-8"><article className="mx-auto max-w-3xl"><Button asChild variant="ghost" className="min-h-11"><Link to="/portal/prayers"><ArrowLeft className="mr-2 h-4 w-4"/>Rudi</Link></Button><Card className="mt-3 rounded-[32px]"><CardContent className="p-6 sm:p-10"><h1 className="text-3xl font-bold sm:text-4xl">{data.title}</h1>{data.summary ? <p className="mt-3 text-lg leading-7 text-muted-foreground">{data.summary}</p> : null}<div className="my-7 h-px bg-border"/><p className="whitespace-pre-wrap text-base leading-8 text-foreground/90">{data.body}</p></CardContent></Card></article></main>; }
