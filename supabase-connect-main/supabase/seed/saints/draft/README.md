# Draft Saints

Place saints here while they are being researched, written, translated, or reviewed.

The CMS import wizard intentionally ignores this folder. Move reviewed content into `../published/` and add the filename to `../manifest.json` before importing.
